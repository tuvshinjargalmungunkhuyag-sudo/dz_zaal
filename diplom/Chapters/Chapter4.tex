% Бүлэг 3 — Хэрэгжүүлэлт

\chapter{ХЭРЭГЖҮҮЛЭЛТ}
\label{Chapter4}
\pagecolor{white}

\noindent\textit{\textbf{Бүлгийн товч агуулга.} Энэ бүлэгт 2-р бүлэгт боловсруулсан системийг бодит үүлэн орчинд байршуулан, иргэдэд хүртээлтэй болгох хэрэгжүүлэлт болон туршилтын үр дүнг харуулна. 3.1 дэд бүлэгт Docker контейнержуулалт, AWS EC2 үүлэн байршуулалт, GitHub Actions CI/CD автоматжуулсан байршуулалтын ажиллагааг тайлбарлана. 3.2 дэд бүлэгт send-code болон verify-code endpoint-ууд, Nodemailer + Gmail SMTP-ийн бодит тохиргоо бүхий email баталгаажуулалтын хэрэгжүүлэлтийг харуулна. 3.3 дэд бүлэгт системийн функциональ туршилт, хариу цагийн гүйцэтгэлийн хэмжилт, давхардал хориглогчийн туршилтын үр дүнг нэгтгэн дүгнэнэ.}

\bigskip

%-------------------------------------------------------------------------------

\section{Үүлэн байршуулалт}

\subsection*{Docker контейнержуулалт ба AWS EC2}

Node.js/Express бэкэнд сервер нь Docker контейнер хэлбэрээр AWS EC2 (ap-southeast-1, Сингапур) дээр ажиллана. Docker ашиглахын гол зорилго нь хөгжүүлэлтийн болон продакшн орчин яг ижил байлгах явдал юм. Бэкэнд нь \code{backend/Dockerfile}-аар тодорхойлогдсон дүрс ашиглана. Node.js 18 Alpine суурь дүрс сонгосноор эцсийн дүрсний хэмжээ жижиг байж, татах болон байршуулах хугацааг хурдасгана.

\begin{lstlisting}[language={}, caption={backend/Dockerfile --- Node.js 18 Alpine суурьтай бэкэндийн дүрс}]
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
\end{lstlisting}

EC2 instance дээр Docker суулгасны дараа ECR-аас дүрс татан контейнер ажиллуулна. \code{--restart unless-stopped} тугаар контейнер дахин эхлүүлэгддэг тул EC2 дахин асах үед бэкэнд автоматаар ажиллана. \code{.env} файл нь GROQ\_API\_KEY, EMAIL\_USER, EMAIL\_PASS болон Firebase тохиргооны нууц мэдээллийг хадгалдаг бөгөөд Git-д оруулдаггүй. Дүрслэлийн архитектурыг доорх диаграммаар харуулав.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners=6pt, minimum width=3.5cm,
              minimum height=1.1cm, align=center, font=\small, thick, fill=gray!12},
  arr/.style={-latex', thick},
  font=\small
]

\draw[dashed, rounded corners=8pt, fill=gray!5]
  (-1.5,-0.2) rectangle (3.5,1.8);
\node[font=\tiny\bfseries, above] at (1,1.8) {Клиент (Хэрэглэгчийн төхөөрөмж)};
\node[box] (phone) at (1,0.8) {Flutter App\\Android / iOS};

\draw[dashed, rounded corners=8pt, fill=gray!8]
  (4.5,-0.2) rectangle (10.5,3.8);
\node[font=\tiny\bfseries, above] at (7.5,3.8) {AWS Cloud (ap-southeast-1)};
\node[box] (ec2box)  at (7.5,2.8) {AWS EC2 Instance\\t3.micro};
\node[box] (docker)  at (7.5,1.5) {Docker Container\\Node.js/Express :3000};
\node[box] (ecrbox)  at (7.5,0.2) {AWS ECR\\Docker Image Registry};

\draw[dashed, rounded corners=8pt, fill=gray!5]
  (-1.5,-4.8) rectangle (3.5,-2.2);
\node[font=\tiny\bfseries, above] at (1,-2.2) {Google Firebase Cloud};
\node[box] (auth)      at (1,-3.2) {Firebase Auth};
\node[box] (firestore) at (1,-4.2) {Cloud Firestore};

\draw[dashed, rounded corners=8pt, fill=gray!5]
  (4.5,-4.8) rectangle (10.5,-2.2);
\node[font=\tiny\bfseries, above] at (7.5,-2.2) {DevOps / CI-CD Pipeline};
\node[box] (github)   at (7.5,-3.2) {GitHub Repository};
\node[box] (actions)  at (7.5,-4.2) {GitHub Actions Workflow};

\draw[arr] (phone.east) -- node[above,font=\tiny]{REST API} (docker.west |- phone.east);
\draw[arr] (phone.south)-- node[left, font=\tiny]{SDK}      (auth.north);
\draw[arr] (docker.south)--node[right,font=\tiny]{pull image}(ecrbox.north);
\draw[arr] (actions.north)--node[right,font=\tiny]{docker push}(ecrbox.south);

\end{tikzpicture}
\caption{Үүлэн байршуулалтын архитектур --- AWS EC2, Firebase, CI/CD\\{\footnotesize Эх: Зохиогчийн боловсруулалт}}
\label{fig:deployment}
\end{figure}

\subsection*{GitHub Actions CI/CD хоолой}

GitHub Actions-ийн CI/CD хоолой нь хөгжүүлэгч \code{backend/} хавтаст өөрчлөлт push хийх бүрт автоматаар ажилладаг. Нийт байршуулалт 3--4 минут болдог бөгөөд хүний гараар сервер дээр нэвтрэх шаардлага байхгүй болдог. Workflow нь 6 алхамтай: checkout, AWS нэвтрэлт тохируулах, ECR нэвтрэх, Docker build хийж ECR-д push хийх, EC2-д SSH-аар холбогдон шинэ дүрс татаж контейнер солих.

\begin{lstlisting}[language={}, caption={GitHub Actions --- автомат байршуулалтын workflow (.github/workflows/deploy.yml)}]
name: Deploy Backend
on:
  push:
    paths: ['backend/**']
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1
      - name: Login and push to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build -t $ECR_REGISTRY/dz-zaal-backend:latest ./backend
          docker push $ECR_REGISTRY/dz-zaal-backend:latest
      - name: Deploy to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          printf '%s\n' "$EC2_SSH_KEY" | tr -d '\r' > /tmp/deploy_key
          chmod 600 /tmp/deploy_key
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no \
            ec2-user@$EC2_HOST "
              docker pull $ECR_REGISTRY/dz-zaal-backend:latest
              docker stop dz-zaal-backend || true
              docker rm   dz-zaal-backend || true
              docker run -d --name dz-zaal-backend \
                --restart unless-stopped \
                --env-file ~/backend/.env \
                -p 3000:3000 \
                $ECR_REGISTRY/dz-zaal-backend:latest"
\end{lstlisting}

CI/CD хоолойн дарааллыг доорх диаграммаар харуулав. Git push-ийн дараа Docker build амжилттай болсон тохиолдолд ECR push болж, EC2-д автоматаар байршуулагддаг.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  cicdstep/.style={draw, rounded corners=5pt, minimum width=3cm,
               minimum height=0.9cm, align=center, font=\small, thick, fill=gray!10},
  dec/.style={draw, diamond, fill=gray!20, aspect=2.2,
              align=center, font=\scriptsize, inner sep=2pt},
  arr/.style={-latex', thick},
  font=\small
]

\node[cicdstep] (push)    at (0,8)    {git push\\backend/};
\node[cicdstep] (trigger) at (0,6.8)  {GH Actions\\Trigger};
\node[cicdstep] (build)   at (0,5.6)  {Docker Build};
\node[dec]      (ok)      at (0,4.2)  {Build\\амжилттай?};
\node[cicdstep] (ecr)     at (0,3.0)  {ECR Push\\(AWS Registry)};
\node[cicdstep] (ssh)     at (0,1.8)  {SSH to EC2};
\node[cicdstep] (pull)    at (0,0.6)  {docker pull\\шинэ image};
\node[cicdstep] (run)     at (0,-0.6) {docker stop old\\docker run new};
\node[cicdstep, fill=gray!25] (fail) at (4,4.2) {Workflow fail\\мэдэгдэл};

\draw[arr] (push)    -- (trigger);
\draw[arr] (trigger) -- (build);
\draw[arr] (build)   -- (ok);
\draw[arr] (ok)      -- node[left,font=\scriptsize]{Тийм}  (ecr);
\draw[arr] (ok)      -- node[above,font=\scriptsize]{Үгүй} (fail);
\draw[arr] (ecr)     -- (ssh);
\draw[arr] (ssh)     -- (pull);
\draw[arr] (pull)    -- (run);

\end{tikzpicture}
\caption{GitHub Actions CI/CD хоолойн дэлгэрэнгүй дарааллын диаграмм\\{\footnotesize Эх: Зохиогчийн боловсруулалт}}
\label{fig:cicd}
\end{figure}

%-------------------------------------------------------------------------------

\section{Email баталгаажуулалтын хэрэгжүүлэлт}

Бүртгэлийн үе шатанд хэрэглэгчийн email хаягийн үнэн зөвийг баталгаажуулах нь системийн аюулгүй байдлыг хангах гол шалгуурын нэг юм. 2-р бүлэгт төлөвлөсөн \code{routes/auth.js} модулийн дотор send-code болон verify-code гэсэн хоёр endpoint-ыг бодит EC2 серверт байршуулсан. Энэ хэсэгт эдгээр endpoint-уудын бодит хэрэгжилт, Nodemailer ба Gmail SMTP-тэй холбогдсон байршуулалтын тохиргоо, баталгаажуулалтын төлөвийн өөрчлөлтийг тайлбарлана.

\subsection*{Send-code endpoint --- Nodemailer + Gmail SMTP}

Хэрэглэгч email болон нууц үгээр бүртгүүлсний дараа клиент апп \code{POST /api/auth/send-code} endpoint руу хүсэлт илгээдэг. Сервер 6 оронтой санамсаргүй тоон код үүсгэж, Firestore-ийн \code{email\_verifications} цуглуулгад \code{expiresAt} талбартайгаар 10 минутын хугацаатай хадгална. Дараа нь Nodemailer-ийн SMTP transporter-ээр Gmail-ийн \code{smtp.gmail.com:587} серверт холбогдож, тухайн код агуулсан HTML email-ийг хэрэглэгчийн хаяг руу илгээнэ. \code{EMAIL\_USER} болон \code{EMAIL\_PASS} (Google App Password) нь EC2 instance дээрх \code{.env} файлд хадгалагддаг тул кодын репозиторид задрахгүй.

\begin{lstlisting}[language={}, caption={routes/auth.js --- Nodemailer ашиглан баталгаажуулах код илгээх}]
router.post('/send-code', async (req, res) => {
  const { email, uid } = req.body;
  const code = Math.floor(100000 + Math.random() * 900000).toString();

  await db.collection('email_verifications').doc(uid).set({
    code, email,
    expiresAt: new Date(Date.now() + 10 * 60 * 1000),
  });

  const transporter = nodemailer.createTransport({
    host: 'smtp.gmail.com', port: 587,
    auth: { user: process.env.EMAIL_USER,
            pass: process.env.EMAIL_PASS },
  });
  await transporter.sendMail({
    from: `"Goviin Sport" <${process.env.EMAIL_USER}>`,
    to: email, subject: 'Goviin Sport - Batalgaajuulakh kod',
    html: `<h2>${code}</h2><p>10 minutiin dotoor huchintei</p>`,
  });
  res.json({ success: true });
});
\end{lstlisting}

\subsection*{Verify-code endpoint ба бүртгэлийн төлөв}

Хэрэглэгч хүлээн авсан 6 оронтой кодыг апп дотор оруулахад клиент \code{POST /api/auth/verify-code} endpoint руу uid болон код илгээнэ. Сервер \code{email\_verifications/\{uid\}} document-ийг уншиж, \code{expiresAt} цаг хүчинтэй эсэх, оруулсан кодын тохирох эсэхийг шалгана. Зөв тохиолдолд Firestore-ийн \code{users/\{uid\}} document-д \code{emailVerified: true} тэмдгийг тавьж, \code{email\_verifications} document-ийг устгана. Хугацаа хэтэрсэн эсвэл код буруу үед хэрэглэгч send-code-г дахин дуудаж шинэ код авах боломжтой.

Бодит туршилтаар Gmail SMTP-ээр илгээгдсэн email хэрэглэгчийн inbox-д дунджаар 4--7 секундийн дотор хүрч ирсэн бөгөөд код шалгах хариу цаг 200~мс орчим байсан. Энэ үр дүн нь дараагийн \textit{Туршилт ба үнэлгээ} хэсэгт нэгтгэгдсэн API-ийн хариу цагийн хэмжилтэд тусгагдсан.

%-------------------------------------------------------------------------------

\section{Туршилт ба үнэлгээ}

Туршилтыг хоёр голлох чиглэлд явуулсан: (1) захиалгын бүрэн мөчлөгийн функциональ туршилт, (2) системийн хариу цагийн гүйцэтгэлийн хэмжилт. Туршилтын орчин: Android 13 (Samsung Galaxy A54), AWS EC2 t3.micro (ap-southeast-1), Firebase Cloud Firestore, 4G LTE сүлжээ.

\subsection*{Туршилт 1 — Захиалгын бүрэн мөчлөгийн функциональ туршилт}

Энэ туршилт нь аппын гол зорилго болох \textit{захиалга хийх бүрэн урсгал}-ыг нэг дамжуулалтаар шалгасан. Бүртгэлээс эхлээд захиалга баталгаажих хүртэлх бүх алхамыг дараалан гүйцэтгэн, Firestore-д хадгалагдсан байдал болон давхардал хориглогчийн зөв ажиллагааг баталгаажуулсан.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
  tbox/.style={draw, rounded corners=5pt, minimum width=3.8cm,
               minimum height=0.85cm, align=center, font=\small, thick, fill=gray!10},
  ok/.style={draw, rounded corners=5pt, minimum width=2.6cm,
             minimum height=0.75cm, align=center, font=\small, thick,
             fill=green!15, text=green!50!black},
  err/.style={draw, rounded corners=5pt, minimum width=3.2cm,
              minimum height=0.75cm, align=center, font=\small, thick,
              fill=red!12, text=red!60!black},
  arr/.style={-latex', thick},
  darr/.style={-latex', thick, dashed, gray},
  font=\small
]

% Main flow — left column
\node[tbox] (reg)    at (0,9.5) {Бүртгэл (email,\\нууц үг)};
\node[tbox] (verify) at (0,8.2) {Email баталгаажуулалт\\(6 оронтой код)};
\node[tbox] (login)  at (0,6.9) {Нэвтрэх};
\node[tbox] (hall)   at (0,5.6) {Заал болон огноо\\сонгох};
\node[tbox] (slot)   at (0,4.3) {Чөлөөт цагийн үе\\сонгох (08:00--24:00)};
\node[tbox] (book)   at (0,3.0) {Захиалга баталгаажуулах};
\node[tbox] (conf)   at (0,1.7) {DBK-xxxxx код авах\\Firestore-д хадгалагдах};
\node[tbox] (cancel) at (0,0.4) {Захиалга цуцлах\\(статус: cancelled)};

% Result badges — right column
\node[ok] (r1) at (5.5,9.5) {\checkmark\ emailVerified: true};
\node[ok] (r2) at (5.5,8.2) {\checkmark\ Код 3--8 сек ирсэн};
\node[ok] (r3) at (5.5,6.9) {\checkmark\ Амжилттай};
\node[ok] (r4) at (5.5,5.6) {\checkmark\ Хуваарь зөв гарсан};
\node[ok] (r5) at (5.5,4.3) {\checkmark\ Чөлөөт цаг харагдсан};
\node[ok] (r6) at (5.5,3.0) {\checkmark\ Захиалга үүссэн};
\node[ok] (r7) at (5.5,1.7) {\checkmark\ Firestore-д бүртгэгдсэн};
\node[ok] (r8) at (5.5,0.4) {\checkmark\ Статус шинэчлэгдсэн};

% Arrows down main flow
\draw[arr] (reg)    -- (verify);
\draw[arr] (verify) -- (login);
\draw[arr] (login)  -- (hall);
\draw[arr] (hall)   -- (slot);
\draw[arr] (slot)   -- (book);
\draw[arr] (book)   -- (conf);
\draw[arr] (conf)   -- (cancel);

% Dashed result arrows
\foreach \s/\r in {reg/r1, verify/r2, login/r3, hall/r4,
                   slot/r5, book/r6, conf/r7, cancel/r8}{
  \draw[darr] (\s.east) -- (\r.west);
}

% Duplicate block test
\node[tbox, fill=yellow!15] (dup) at (0,-1.1) {Захиалагдсан цагт\\дахин захиалах};
\node[err]                  (re)  at (5.5,-1.1) {\texttimes\ ``Уг цаг авагдсан байна''};
\draw[arr]  (cancel) -- (dup);
\draw[darr] (dup.east) -- (re.west);

\end{tikzpicture}
\caption{Захиалгын бүрэн мөчлөгийн функциональ туршилтын дарааллын диаграмм\\{\footnotesize Эх: Зохиогчийн боловсруулалт}}
\label{fig:test_functional}
\end{figure}

Туршилтын бүх алхам амжилттай давсан. Давхардал хориглогчийн туршилтад хоёр хэрэглэгч яг нэгэн зэрэг ижил цагт захиалах нөхцөлийг үүсгэсэн бөгөөд Firestore-ийн атомик transaction механизм зөвхөн нэг захиалгыг баталгаажуулж, нөгөөд нь ``Уг цаг авагдсан байна'' алдаа буцаасан.

\subsection*{Туршилт 2 — Системийн хариу цагийн гүйцэтгэлийн хэмжилт}

Системийн үндсэн үйлдэл бүрийн хариу цагийг 4G LTE сүлжээн дээр 10 удаа давтан хэмжиж дунджийг гаргасан. Шаардлагын хязгаартай харьцуулалтыг доорх диаграммд харуулав.

\begin{figure}[h]
\centering
\begin{tikzpicture}[font=\small]
\begin{axis}[
  xbar,
  width=12cm, height=7cm,
  bar width=9pt,
  xmin=0, xmax=5.8,
  xlabel={Хариу цаг (секунд)},
  ytick={1,2,3,4,5},
  yticklabels={
    {AI чатбот (Groq)},
    {Захиалга үүсгэх},
    {EC2 API хариулт},
    {Firestore хүсэлт},
    {Апп ачаалах}
  },
  y tick label style={font=\small, align=right, text width=3.8cm},
  nodes near coords,
  nodes near coords align={horizontal},
  every node near coord/.append style={font=\scriptsize},
  enlarge y limits=0.15,
  legend style={at={(0.98,0.05)}, anchor=south east, font=\small},
  legend cell align=left,
]
\addplot[fill=gray!50, draw=gray!70] coordinates {
  (0.9, 1)
  (1.1, 2)
  (0.8, 3)
  (0.6, 4)
  (2.3, 5)
};
\addplot[fill=red!15, draw=red!60, dashed, line width=1pt] coordinates {
  (5.0, 1)
  (2.0, 2)
  (2.0, 3)
  (2.0, 4)
  (3.0, 5)
};
\legend{Хэмжсэн дундаж (с), Шаардлагын хязгаар (с)}
\end{axis}
\end{tikzpicture}
\caption{Системийн хариу цагийн хэмжилт ба шаардлагатай харьцуулалт (4G LTE, $n=10$)\\{\footnotesize Эх: Зохиогчийн боловсруулалт}}
\label{fig:test_performance}
\end{figure}

Бүх үзүүлэлт шаардлагын хязгаарыг хангасан. Апп анх ачаалах 2.3 секунд нь Firebase SDK болон локал мэдэгдлийн инициализаци зэрэг явагдахтай холбоотой боловч 3.0 секундын шаардлагад нийцсэн. AI чатботын хариу цаг 0.9 секунд буюу 5.0 секундын хязгаараас хавьгүй доогуур байсан нь Groq-ийн Llama загварын хурдны давуу талыг харуулсан.

\subsection*{Туршилтын дүгнэлт}

Хоёр туршилтын үр дүнгээс системийн үндсэн функциональ шаардлага бүрэн хангагдаж, гүйцэтгэлийн бүх үзүүлэлт тогтоосон хязгаарт нийцсэн болохыг тогтоов. Туршилтын явцад илэрсэн нэг сайжруулах санал: EC2-ийн HTTP холболтыг HTTPS болгон шинэчлэх бөгөөд үүнийг цаашдын хөгжлийн шатанд хэрэгжүүлэхээр зорилтот болгосон.
